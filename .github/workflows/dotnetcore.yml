name: Templates integrity
on: [push]

jobs:
  build:

    runs-on: [windows-latest]

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.100
    - name: dotnet build template sources
      run: |
        dotnet build templateSources/WindowsService/*.sln --configuration Release
        dotnet build templateSources/EFDataLayer/*.sln --configuration Release
        dotnet build templateSources/WpfApplication/*.sln --configuration Release
